//import React from 'react';
//import mainSocialLinks from '@config/constants.js'


//const FooterLinks = () => {
  //return (
    //{ mainSocialLinks.map(({id, to, src,}) => <img key={id} to={to} src={src} />)
  //);
//}

//export default FooterLinks;
